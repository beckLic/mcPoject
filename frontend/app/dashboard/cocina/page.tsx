import SectionContainer from '@/components/SectionContainer';

export default function CocinaPage() {
  return (
    <SectionContainer title="Cocina">
      <p>Aquí irán las tarjetas de hamburguesas y pedidos...</p>
    </SectionContainer>
  );
}