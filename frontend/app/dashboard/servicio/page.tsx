import SectionContainer from '@/components/SectionContainer';

export default function CocinaPage() {
  return (
    <SectionContainer title="Servicio">
      <p>Aquí irán las tarjetas de pedidos...</p>
    </SectionContainer>
  );
}