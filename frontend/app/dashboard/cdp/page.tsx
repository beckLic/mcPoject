import SectionContainer from '@/components/SectionContainer';

export default function CocinaPage() {
  return (
    <SectionContainer title="CDP">
      <p>Aquí irán las tarjetas de helados y pedidos...</p>
    </SectionContainer>
  );
}